import { AgeRangeDirective } from './age-range.directive';

describe('AgeRangeDirective', () => {
  it('should create an instance', () => {
    const directive = new AgeRangeDirective();
    expect(directive).toBeTruthy();
  });
});
