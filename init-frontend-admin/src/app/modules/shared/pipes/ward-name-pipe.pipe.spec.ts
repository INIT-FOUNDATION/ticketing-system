import { WardNamePipePipe } from './ward-name-pipe.pipe';

describe('WardNamePipePipe', () => {
  it('create an instance', () => {
    const pipe = new WardNamePipePipe();
    expect(pipe).toBeTruthy();
  });
});
