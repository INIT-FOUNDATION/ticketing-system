import { RangeLengthDirective } from './range-length.directive';

describe('RangeLengthDirective', () => {
  it('should create an instance', () => {
    const directive = new RangeLengthDirective();
    expect(directive).toBeTruthy();
  });
});
