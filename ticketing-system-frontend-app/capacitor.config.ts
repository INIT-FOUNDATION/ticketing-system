import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'aieze.ticketing.system',
  appName: 'Ticketing System',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
